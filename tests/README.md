# Tests

Automated verification lives here and maps back to requirements.

The first tests will be introduced by `capability/hello-game-world` and will verify the documented launch path, in-world greeting, visible prompt, and lack of runtime AI/network dependency.

See [Test Strategy](../docs/implementation/TEST_STRATEGY.md) and [Traceability](../docs/requirements/TRACEABILITY.md).
